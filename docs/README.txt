# Instructions for ubuntu 22.04

- execute `chmod 777 doc`

- run ./compile.sh

- spec.pdf file is ready in doc directory

# TODO

- make mermaid-filter use svg instead of png when embedding graphics
That requires package librsvg2-bin. I installed it and
the quality if images improved a lot but the text on graphs disappeared.
